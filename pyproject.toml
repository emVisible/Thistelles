[project]
name = "thistelles"
version = "0.12.0"
description = "macOS 全局快捷键语音输入 — 录音即转文字，直达剪贴板"
readme = "README.md"
authors = [
    { name = "Thistelles" },
]
license = "MIT"
requires-python = ">=3.10"
dependencies = [
    "pyaudio>=0.2.14",
    "rumps>=0.4.0",
    "pyobjc-framework-Quartz>=10.0",
    "pyobjc-framework-ApplicationServices>=10.0",
    "pyobjc-framework-AVFoundation>=10.0",
    "pyperclip>=1.8.2",
    "opencc-python-reimplemented>=0.1.7",
    "mlx-whisper>=0.4.0",
    "pyobjc-framework-UserNotifications>=10.0",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: MacOS X :: Cocoa",
    "Operating System :: MacOS :: MacOS X",
    "Topic :: Multimedia :: Sound/Audio :: Speech",
    "Natural Language :: Chinese (Simplified)",
]

[project.urls]
Homepage = "https://github.com/emVisible/Thistelles"
Repository = "https://github.com/emVisible/Thistelles"
Issues = "https://github.com/emVisible/Thistelles/issues"

[project.scripts]
thistelles = "thistelles.cli:entry"

[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[tool.uv]
package = true

[tool.setuptools.packages.find]
where = ["."]
include = ["thistelles*"]

[tool.setuptools.package-data]
thistelles = ["assets/*"]
